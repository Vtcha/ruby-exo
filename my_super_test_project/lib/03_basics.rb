def biggest
	bigger(a,b,c)
    if (a == nil || b == nil || c == nil)
        return "nil detected"
        
        elsif (a > b && a > c)
            return "a is bigger"
            
        elsif(b > a && b > c )
            return "b is bigger"
            
        elsif (c > a && c > b)
            return " c is bigger"

end

def reverse

    classe([])
    return classe([]).reverse!.upcase

    
    end


end

def crazy stuff
    
    magic_array([ ])
    return arr.flatten.sort!.uniq.map { |x| if (x%3 !=0) then  x*2
    end }.compact
end
    
